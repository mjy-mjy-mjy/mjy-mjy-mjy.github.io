---
layout: layouts/base.njk
---
<section class="page-hero">
  <div class="shell page-hero__inner">
    <p class="eyebrow">{{ eyebrow or "MUJIAN NOTES" }}</p>
    <h1>{{ title }}</h1>
    {% if description %}<p>{{ description }}</p>{% endif %}
  </div>
</section>
<div class="shell prose page-content">
  {{ content | safe }}
</div>
